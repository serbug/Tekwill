public class Main {
    public static void main(String[] args) {
        /* Condition: Write a class named User. It must have three string fields: login,firstName,lastName and a constructor with three parameters to initialize these
        * fields.
        * Create a instance of User class inside main method.
        * */

        User user=new User();

        System.out.println("\nLogin: "+user.login + "\nFirstName: "+user.firstName+"\nLastName: "+user.lastName);
    }
}